import { StyleSheet } from 'react-native';

import { Spacing } from '@/constants/theme';

export const signupLawyerSharedStyles = StyleSheet.create({
  fields: {
    gap: Spacing.sm,
    width: '100%',
  },
});
