import { StyleSheet } from 'react-native';

import { Spacing } from '@/constants/theme';

export const signupClientSharedStyles = StyleSheet.create({
  fields: {
    gap: Spacing.sm,
    width: '100%',
  },
});
